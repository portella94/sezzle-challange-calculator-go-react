# Base URL of the calculator API. Defaults to "/api", which is proxied to the
# Go backend by Vite in development and by nginx in the Docker image.
# Override only if the API is hosted elsewhere.
# VITE_API_BASE_URL=/api
