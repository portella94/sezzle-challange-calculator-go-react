// Package version exposes the service's semantic version.
//
// The project follows SemVer (https://semver.org). It starts at 0.0.1; the
// bump to 1.0.0 is performed manually by the maintainer.
package version

// Version is the current semantic version of the backend service.
const Version = "0.0.1"
